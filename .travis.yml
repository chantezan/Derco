
language: node_js
node_js:
  - 14
dist: xenial
#Tell Travis that we ndddeed super user permissions  

script:
- echo "skipping tests"


deploy:
  provider: lambda
  function_name: "pruebaDerco"
  region: "us-east-1"
  runtime: "nodejs12.x"
  handler_name: "handler"
  role: "arn:aws:iam::578807463618:role/service-role/rolLamda"
  access_key_id: $access_key_id
  secret_access_key: $secret_access_key
  on:
    branch: main